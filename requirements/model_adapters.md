# Optional Model Adapter Requirements

Install these one at a time in a fresh notebook/session. Do not install every heavy stack together unless necessary; TTS repos often have conflicting Torch/audio dependencies.
